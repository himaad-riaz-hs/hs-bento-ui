/**
 * Inline `fontFamily` for feature demos — resolves to `--hs-typeface-base-font-family`
 * (`src/tokens/index.css`). Prefer Tailwind `font-sans` / typeface tokens in new UI.
 */
export const HS_FONT_FAMILY = "var(--hs-typeface-base-font-family)";
